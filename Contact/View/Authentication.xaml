<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Contact.View.Authentication">
    <ContentPage.Content>
        <StackLayout Margin="20"
                     VerticalOptions="StartAndExpand">
            <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                <Label Text="Temperature in Shanghai: "></Label>
                <Label x:Name="weather" Text=""></Label>
            </StackLayout>
            
            <StackLayout HorizontalOptions="FillAndExpand"
                         VerticalOptions="Center">
                <Label Text="Password" />
                <Entry x:Name="password" Text="{Binding Password}" HorizontalOptions="FillAndExpand" />
            </StackLayout>

            <Button Text="Submit"
                    Clicked="OnSubmit"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>